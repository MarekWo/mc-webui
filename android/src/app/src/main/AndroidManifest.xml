<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- QR scanning in Add Contact; the page asks first, Android asks second -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- New message alerts. Also what puts the app in Android's notification
         settings at all - without it the app is not even listed there -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Keeps the process alive while the app is in the background, so the page
         behind the WebView can still raise those alerts. Only ever started when
         notifications are switched on in mc-webui itself -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
    <!-- Saving downloads (e.g. database backups) before Android 10 -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- A phone without a camera can still do everything else -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- No exported entry point: the activity is the only thing that starts
             it, and only while the page asks for notifications -->
        <service
            android:name=".MeshWatchService"
            android:exported="false"
            android:foregroundServiceType="remoteMessaging" />
    </application>

</manifest>
